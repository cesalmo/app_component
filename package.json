{
  "name": "web-app",
  "version": "0.0.0",
  "description": "Webcomponent web-app following open-wc recommendations",
  "author": "web-app",
  "license": "MIT",
  "scripts": {
    "serve": "es-dev-server --app-index index.html --node-resolve --watch --open",
    "serve_parcel": "parcel serve index.html",
    "build_parcel": "parcel build index.html --no-minify"
  },
  "dependencies": {
    "@ui5/webcomponents": "^1.0.0-rc.4",
    "lit-html": "^1.0.0"
  },
  "devDependencies": {
    "es-dev-server": "^1.26.1",
    "parcel-bundler": "^1.12.4"
  }
}
